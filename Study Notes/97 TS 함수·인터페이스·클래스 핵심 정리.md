# TS 함수·인터페이스·클래스 핵심 정리

### 📅 날짜:

> 2025.11.24 (월)
> 

### 📘 오늘 공부한 주제:

> 함수 타입, 인터페이스 vs 타입 별칭, 클래스 타입스크립트 적용
> 

---

## 📝 핵심 개념 요약

- 함수 타입 표현식 `(a: number) => string` 을 사용해 함수 형태 자체를 타입으로 정의할 수 있음
- 호출 시그니처(Call Signature)를 사용하면 “속성이 있는 함수 객체” 타입도 정의 가능
- 매개변수는 더 적게 받을수록 호환되고, 반환 타입은 더 구체적일수록 호환됨
- 함수 오버로딩은 여러 버전의 함수 시그니처를 선언하고 실제 구현은 하나만 작성
- Interface vs Type Alias 차이: 확장 방식, 선언 병합 가능 여부, 유틸리티 타입 사용 범위
- 클래스에서 public/private/protected 등 접근 제어자를 사용하여 타입 안전성 증가
- 매개변수 속성(parameter properties)으로 생성자 안 코드 축약 가능
- 추상 클래스는 공통 규약 제공 + 강제 구현 구조
- 인터페이스는 클래스가 반드시 구현해야 하는 구조를 강제하는 “계약서” 역할

## 📊 핵심 요약 표

| 개념 | 설명 | 예시 |
| --- | --- | --- |
| 함수 타입 표현식 | `(a: number) => string` 형태로 함수 타입 정의 | `type Handler = (msg: string) => void` |
| 호출 시그니처 | 함수 객체 + 속성을 같이 정의 | `{ desc: string; (x: number): boolean }` |
| 매개변수 규칙 | 적을수록 호환 ↑ | `(a) => {}` 가능 |
| 반환 타입 규칙 | 더 구체적일수록 호환 ↑ | `Dog → Animal` |
| 오버로딩 | 여러 시그니처 + 하나의 구현 | `function fn(a: string): ...` |
| 인터페이스 확장 | `extends` | `interface Dog extends Animal` |
| 타입 별칭 확장 | `&` 연산자 | `type Dog = Animal & {...}` |
| 선언 병합 | 인터페이스만 가능 | `interface User {}` |
| 클래스 타입 | public/private/protected/readonly | `private age: number` |
| 매개변수 속성 | 생성자 축약 | `constructor(public name: string)` |
| 추상 클래스 | 규약 제공, 인스턴스 불가 | `abstract getArea()` |
| 인터페이스 + 클래스 | `implements` 로 구조 강제 | `class Duck implements Flyable` |

### 💻 실습 내용 정리

### ✔ 실습: 블로그 액션 함수 타입 정의

- `src/types.ts`에서 5가지 블로그 액션 함수 타입 생성
- 예: `CreatePostAction`, `DeletePostAction`, `UpdatePostAction` 등

### ✔ 사용자 정의 타입 가드 3개 구현

- `src/postUtils.ts`에서
    - `isDraftPost(post)`
    - `isPublishedPost(post)`
    - `isArchivedPost(post)`
        
        구현 완료
        

### ✔ 발행된 포스트의 제목만 출력

- `src/index.ts`
    - `posts.filter(isPublishedPost).map((p) => console.log(p.title))` 형태로 구현

---

### ✔ 인터페이스 변환 실습

- 타입 별칭 → 인터페이스로 변경
    - `User`, `DraftPost`, `PublishedPost`, `ArchivedPost` 모두 interface로 변환
- `Author extends User` 인터페이스 작성
    - `bio?: string;`
    - `posts: Post[];`

---

### ✔ BlogService 클래스 구현

- `private posts: Post[] = []`
- `private nextPostId = 1`
- `getAllPosts()` 반환
- `getPostById(id)` 검색
- `addPost(postData)` 생성 후 status 자동 처리를 포함
- `IBlogService` 인터페이스 작성 → `BlogService implements IBlogService` 적용

### ❗ 헷갈렸던 점 / 문제 해결:

| 헷갈린 점 | 문제 해결 |
| --- | --- |
| 함수 타입 할당 시 매개변수 수가 다르면 왜 오류날까? | TS는 “매개변수는 적을수록 호환된다” 규칙을 적용 → 함수 안전성 보장 |
| 함수 오버로딩은 왜 구현부를 하나만 써야 할까? | JS로 컴파일되면 하나의 함수만 존재하기 때문 |
| Type Alias와 Interface 확장 차이가 헷갈림 | Interface는 `extends`, Type Alias는 `&` 로 정리 |
| 클래스 접근 제어자가 JS에 없는 문법이라 헷갈림 | TS 전용 문법이며 런타임에서는 제거됨. private 필드는 JS `#` 문법과는 별도로 존재함 |
| 추상 클래스와 인터페이스의 차이 | 인터페이스는 “형태만”, 추상 클래스는 “기본 기능 + 강제 규약” 제공 |

### 💡 느낀 점 / 배운 점:

- 함수 타입 시스템의 호환성 규칙(매개변수는 적게, 반환 타입은 더 구체적으로)이 이제 명확해짐
- 인터페이스와 타입 별칭의 차이점이 단순 문법이 아닌 “설계 방식의 차이”라는 것을 이해함
- 클래스 문법에서 TS가 제공하는 타입 안정성(접근 제한자, 추상 클래스 등)이 JS 코드보다 훨씬 구조적임을 느낌
- 실제 실습하면서 “인터페이스로 계약을 걸고 클래스에서 구현하는 구조”가 매우 깔끔하고 확장성이 좋다고 느껴짐

### 🏷️ 키워드 (태그):

`#FunctionType` `#callSingnature` `#Overloading` `#InterFace` `#BlogService` `#Implements` `#TypeGuard` `#ProductType` `#DiscriminatedUnion`

| 날짜 | 주제 | 핵심 요약 | 실습 내용 | 문제 해결 / 느낀 점 | 키워드 태그 | 복습 필요 |
| --- | --- | --- | --- | --- | --- | --- |
| 2025-11-24 | 함수 타입/인터페이스/클래스 | 함수 타입 시스템, 인터페이스 vs 타입, 클래스 타입 적용, 접근 제어자, 추상 클래스, implements | 액션 타입 정의, 타입 가드 작성, BlogService 구현 | 함수 타입 호환 규칙/인터페이스와 타입 차이/클래스 타입 안정성 이해 | `#FunctionType` `#Interface` `#Class` | ✅ |
